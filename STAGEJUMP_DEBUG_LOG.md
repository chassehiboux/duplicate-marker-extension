# StageJump Debug Session

Дата: 2026-02-20

## Шаги
- [x] Шаг 1: Подготовка и чтение правил (AGENTS.md, STAGEJUMP_RULES.md, playwight skill).
- [x] Шаг 2: Локализация кода StageJump и автофильтра в content.js.
- [x] Шаг 3: Полный цикл в браузере /ovzid/status/all -> переход в стадию.
- [x] Шаг 4: Сбор фактов (логи, postData, состояние StageTimer) и определение причины.
- [x] Шаг 5: Правка кода и синхронизация правил.
- [x] Шаг 6: Повторная проверка цикла и фиксация результата в новой сессии Playwright.
- [x] Шаг 7: Проверка после полной переустановки расширения пользователем.
- [x] Шаг 8: Повторная локализация причины по телеметрии gate (почему фильтр не отправляется).
- [x] Шаг 9: Изменение механизма применения автофильтра (через input+Enter, one-shot).
- [x] Шаг 10: Финальная проверка уже обновлённой версии расширения (после перезагрузки/переустановки с новым `content.js`).
- [x] Шаг 11: Добавить кнопку StageJump в автоскрытие при скриншоте.
- [x] Шаг 12: Добавить dropdown-действия на `/ovzid/status/all` и автофильтр DebtID на `/bus/slowsearch`.
- [x] Шаг 13: Убрать CSP-зависимость inline-bridge и перевести применение DebtID на `MAIN world` через background.
- [ ] Шаг 14: Повторная live-верификация после фактической перезагрузки расширения в браузере.

## Журнал действий
1. Прочитаны AGENTS.md, STAGEJUMP_RULES.md и skill playwright.
2. Локализованы ключевые функции в content.js: waitForStageTimerGate, applyStageJumpDebtIdFilterOnce, runAttempt.
3. Запуск полного цикла в браузере: /ovzid/status/all -> фильтр ЛС -> клик StageJump.
3.1 Открыта страница https://kgn.pyramid.vostok-electra.ru/ovzid/status/all (чистая сессия Playwright).
3.2 Для фильтра на /ovzid/status/all надо делать клик в фильтр ЛС и Enter.
3.5 После Enter появились строки в гриде /ovzid/status/all, кнопки StageJump (→) присутствуют в cbox-колонке.
3.6 Начинаю проверку перехода: клик по первой доступной кнопке StageJump.
4. Факт: переход StageJump на целевую страницу работает, но автофильтр не закрепляется (таймаут 60с).
5. Проверка вручную в консоли: тот же путь setGridParam + reloadGrid с DebtID=554117 работает корректно.
6. Вывод: проблема в gate-ветке ожидания (runAttempt), а не в jqGrid API.
7. Внесена правка в content.js: добавлен hard fallback после STAGE_TIMER_LOADING_FALLBACK_MS при отрисованном гриде, чтобы запускать one-shot фильтр и не зависать в wait.
8. Обновлен AGENTS.md: добавлено правило обязательной сверки с последним шагом журнала STAGEJUMP_DEBUG_LOG.md.
9. Для проверки новой ветки добавлен отдельный warning-лог: "Принудительный fallback gate...".
10. Повторный прогон после правки в текущем браузерном контексте показал старое поведение (таймаут 60с, без нового лога hard fallback).
11. Вывод: активная сессия браузера использует ранее загруженную версию расширения; для проверки новой логики требуется перезагрузка расширения/новый браузерный процесс.
12. Выполнен новый прогон в новой сессии Playwright (после `browser_close`): `/ovzid/status/all` -> фильтр ЛС -> клик `→` для DebtID=554117.
13. Результат повторился: на целевой странице `https://.../ovzid/status/100091?dup_stage_jump_debtid=554117#...` в `postData` снова `_search=false`, `filters=null`, поле `gs_DebtID` пустое.
14. В консоли снова только ожидание StageTimer и финальный таймаут `Не удалось применить DebtID в течение 60 секунд`, без лога `Принудительный fallback gate`.
15. Проверка вручную через `jqGrid('setGridParam' + reloadGrid)` снова успешна: `_search=true`, `filters` с `DebtID=554117` выставляются сразу.
16. Дополнительно зафиксирован технический лимит: в этом Playwright-контексте доступ к `chrome://extensions` заблокирован, поэтому принудительно перезагрузить установленное расширение из UI нельзя.
17. Рабочая гипотеза подтверждена: тестируемая в браузере копия extension не подхватывает локальные изменения `content.js`, из-за чего поведение остаётся прежним.
18. После сообщения пользователя о полной переустановке расширения выполнен новый полный цикл: `/ovzid/status/all` -> фильтр `ЛС` (клик + Enter) -> переход `→` с `DebtID=554117`.
19. Результат после перехода снова воспроизвел проблему: `postData._search=false`, `postData.filters=null`, автофильтр не закреплялся, через ~60 секунд приходил таймаут StageJump.
20. Дополнительно снята телеметрия логики gate (реплика `getStageTimerGateState`): состояние стабильно доходит до `loading_completed`, то есть блокировка по StageTimer больше не является узким местом.
21. Ручная проверка в странице подтвердила, что `setGridParam + reloadGrid` работает и выставляет `_search=true` и `filters` с `DebtID`.
22. Отдельная ручная проверка (чисто через DOM): заполнение `gs_DebtID` + dispatch `input/change` + `Enter` также стабильно применяет фильтр (`_search=true`, `filters` заполняется).
23. Уточнена причина: в контексте content-script путь через прямой вызов jqGrid оказался ненадёжным, из-за чего автофильтр не доходил до фактического запуска.
24. В `content.js` обновлён `applyStageJumpDebtIdFilterOnce`: основной путь теперь `input + Enter` (однократно, после `Загрузка`), jqGrid-вызов оставлен только как fallback.
25. После полной переустановки расширения выполнен контрольный прогон: `/ovzid/status/all` -> фильтр `ЛС` (клик + Enter) -> переход `→` по строке `DebtID=554117`.
26. На целевой странице зафиксированы логи: `Найден параметр DebtID...`, `Ждём завершения основной "Загрузка"...`, затем `DebtID отправлен в фильтр (однократно)` и `DebtID применен один раз`.
27. Проверка состояния грида после перехода: `postData._search=true`, `postData.filters` содержит правило по `DebtID=554117`, в видимых строках остался только `DebtID 554117`.
28. Дополнительное ожидание ~50 секунд после применения показало отсутствие повторных запусков: число console-сообщений не выросло, новых `DebtID отправлен...` не появилось.
29. По запросу пользователя усилено автоскрытие кнопки StageJump при скриншоте: в `content.js` добавлен отдельный маркер `data-dup-stage-jump-btn="1"` и включён в селекторы screenshot-hide.
30. Кнопка StageJump теперь получает маркер при создании (`createStageJumpButton`), чтобы автоскрытие работало даже при изменении классов кнопки.
31. Для унификации поведения добавлены селекторы скрытия `.dup-stage-jump-btn` и `[data-dup-stage-jump-btn="1"]` в `StageTimer/timer_styles.css`.
32. По уточнению пользователя убрана логика выбора департамента через grid-фильтр (`gs_list_Departmentname`) для `/bus/slowsearch`.
33. В `content.js` добавлен выбор департамента только через верхний переключатель `btn-cities dropdown-toggle` и пункты `a.department-switch`.
34. Для перехода StageJump на slowsearch URL теперь собирается из `href` нужного `department-switch`: сохраняется `/login/department/{id}`, а путь заменяется на `/ovzid/status/all`.
35. В общий запуск добавлен `initSlowsearchJumpButtons()`, выполнена проверка синтаксиса: `node --check content.js` (OK).
36. Добавлена усиленная очистка DebtID после успешной автофильтрации: `clearStageJumpDebtIdFilterInput` теперь очищает все возможные поля DebtID/DepID в строке фильтров, а не только первый найденный input.
37. Синтаксис проверен: `node --check content.js` (OK).
38. По результатам live-прогона выявлен сценарий: фильтр в `postData` уже есть, но `finishSuccess` не наступает и цикл уходит в таймаут 60с.
39. Добавлен защитный фолбэк `hasStageJumpDebtIdInRawPostData` (поиск DebtID в сыром `postData.filters`) и включён в `isStageJumpDebtIdApplied`, чтобы завершение и очистка не зависели от варианта `op`/формата rules.
40. Синтаксис после фикса проверен: `node --check content.js` (OK).
41. Диагностика показала, что после input+Enter фильтр DebtID применялся как `op=cn`, и в грид попадали похожие DebtID (например, `7587445`), из-за чего строгая проверка по видимым строкам не давала `finishSuccess`.
42. Добавлены функции `hasAnyVisibleRowWithStageJumpDebtId`, `hasStageJumpDebtIdInFilterInput` и `isStageJumpDebtIdAppliedAfterAttempt`; в ветке `attemptedApply` переключена проверка на новую, чтобы завершать цикл при наличии точного DebtID в видимых строках и заполненном input.
43. Синтаксис после правки проверен: `node --check content.js` (OK).
44. Для верификации новой версии перезапущен Playwright-браузер; после рестарта в логах StageJump появились новые номера строк (`content.js:1662/1726/1728/1750`), что подтвердило подхват актуального `content.js`.
45. Повторный полный цикл: `/bus/slowsearch` -> фильтр `DebtID=587445` -> клик `→` -> переход на `/ovzid/status/all?dup_stage_jump_debtid=587445`.
46. На целевой странице получен лог `DebtID применен один раз`.
47. Факт очистки подтверждён: URL очищен до `/ovzid/status/all` (без search/hash), поле `gs_list_DebtID` пустое.
48. По новой задаче на `/ovzid/status/all` кнопка StageJump переведена в dropdown с 2 действиями: `Переход в Глобальный поиск` и `Переход к ИД на стадии`.
49. Действие `Переход в Глобальный поиск` использует `openStageJumpTarget('/bus/slowsearch', debtId)` и передаёт `dup_stage_jump_debtid` в URL/hash + storage.
50. Добавлена функция `initSlowsearchDebtIdFilterFromHash()` для страницы `/bus/slowsearch`: после готовности грида выполняется one-shot применение `DebtID` через input+Enter.
51. После успешного применения на `/bus/slowsearch` выполняются очистка поля DebtID и очистка параметра `dup_stage_jump_debtid` из URL/hash (`clearStageJumpDebtIdFromHash`).
52. В общий блок запуска добавлен вызов `initSlowsearchDebtIdFilterFromHash()`.
53. Синтаксис проверен локально: `node --check content.js` (OK).
54. Проведён отдельный анализ стадии `/ovzid/PrepareToReturn` (длительная основная загрузка): при открытии стартует запрос `.../PrepareToReturn/data?_search=false...`, `grid.p.jqXhr.readyState=1`, таймер в состоянии `Загрузка`.
55. Проверка гипотезы «фильтр во время загрузки»: ввод в `ЛС` + Enter во время `Загрузка` не приводит к реальному фильтрующему запросу; в сеть уходит только базовый `_search=false`, после завершения остаются нефильтрованные данные.
56. Проверка через `setGridParam + reloadGrid` во время `Загрузка` дала тот же результат: `postData.filters` заполняется в клиенте, но фильтрующий HTTP-запрос не отправляется до завершения основного load.
57. Проверка «сброса загрузки» через `grid.p.jqXhr.abort()` успешна: текущий запрос получает `ERR_ABORTED`, затем новый запрос с `_search=true&filters=...` уходит и возвращает `200`.
58. Итог по `/ovzid/PrepareToReturn`: начать фильтрацию «сразу» можно только через принудительный сброс текущего `jqXhr` и повторный запуск фильтра; без `abort` фильтр, отправленный в фазе `Загрузка`, не срабатывает фактически.
59. По уточнению пользователя отменён page-specific подход: ускоренная схема `abort + one-shot фильтр` перенесена в общую логику перехода StageJump (переход с `/ovzid/status/all` на целевую стадию), без привязки к конкретной странице.
60. В `initStageJumpDebtIdFilterFromHash()` удалено ожидание `StageTimer gate`: теперь алгоритм единый — один раз прерываем активный `jqXhr`, ждём освобождения грида и сразу отправляем автофильтр DebtID.
61. Удалены неиспользуемые gate-хелперы (`getStageTimerSnapshot`, `getStageTimerGateState`) и служебные состояния `lastTimerWaitReason/stageTimerGateState`.
62. Проверка синтаксиса после правки: `node --check content.js` (OK).
63. После обновления расширения выполнен live-прогон: `/ovzid/status/all` -> клик в фильтр `ЛС` -> `Enter` -> строки загрузились, кнопки `↔` присутствуют.
64. Выполнен переход по строке `DebtID=554117`: `↔` -> `Переход к ИД на стадии`, открыта новая вкладка на `/ovzid/status/100091`.
65. На целевой вкладке зафиксированы логи расширения: `Найден параметр DebtID...`, затем один `DebtID отправлен в фильтр (однократно)`, затем `DebtID применен один раз`.
66. Повторного цикла отправки нет: в console только один `DebtID отправлен...`, ошибок/предупреждений не обнаружено.
67. Параметр перехода очищается корректно: `window.location.href = https://.../ovzid/status/100091` (без `dup_stage_jump_debtid` в `search/hash`).
68. Доп. наблюдение: фильтр в `postData` выставился как `op=cn` (contains), поэтому в гриде остаются похожие DebtID; точный `eq` в этом прогоне не принудился.
69. В `background.js` добавлен action `STAGEJUMP_APPLY_DEBTID_MAIN_WORLD`: через `chrome.scripting.executeScript({ world: 'MAIN' })` вызывается `jqGrid('setGridParam' + 'reloadGrid')` с фильтром `DebtID` строго `op='eq'`.
70. В `content.js` функция `applyStageJumpDebtIdFilterViaPageBridge` переведена на отправку `chrome.runtime.sendMessage` в background вместо inline `<script>` (устранение CSP-блокировки).
71. В `content.js` убран вводящий в заблуждение лог `page-bridge (eq)`; теперь подтверждение успеха приходит из callback ответа background-bridge.
72. Локальная проверка после правки: `node --check content.js` и `node --check background.js` — OK.
73. Live-прогон в текущем браузерном контексте (`/ovzid/status/all` -> `↔` -> `Переход к ИД на стадии`) показал старые логи: CSP-ошибка `Executing inline script violates CSP` на `content.js:1769` и старый one-shot путь.
74. Факт из п.73 означает, что в активном браузере осталась предыдущая сборка расширения (новый bridge через background не подхвачен), поэтому результат live-прогона не отражает текущий код.
75. Следующий обязательный шаг: перезагрузить/обновить unpacked extension и повторить цикл для фиксации `op='eq'` и отсутствия повторной автофильтрации.
76. После сообщения пользователя «Обновил расширение» выполнен новый live-прогон: `/ovzid/status/all` -> фильтр `ЛС` (клик + Enter) -> `↔` по `DebtID=554117` -> `Переход к ИД на стадии`.
77. На целевой вкладке `/ovzid/status/100091?...dup_stage_jump_debtid=554117` зафиксированы логи `DebtID отправлен...`, но затем повторно воспроизвелся warning `Не удалось применить DebtID в течение 60 секунд`.
78. Диагностика в странице показала: `postData._search=true` и `postData.filters` уже содержит `DebtID` с `op=eq`, то есть фильтр фактически отправлялся, а timeout возникал в контрольной ветке ожидания.
79. Выявлена причина: после первого apply цикл продолжал ждать «подтверждение» до 60с и при длительной занятости jqGrid уходил в ложный fail вместо завершения one-shot сценария.
80. В `content.js` обновлена логика `initStageJumpDebtIdFilterFromHash`: после первого успешного `applyStageJumpDebtIdFilterOnce` выполняется немедленный `finishSuccess()` (очистка поля/хэша и остановка цикла), без повторных попыток.
81. Аналогично обновлена `initSlowsearchDebtIdFilterFromHash`: режим one-shot `apply -> finishSuccess`, без повторного цикла после отправки фильтра.
82. Локальная проверка синтаксиса после правки: `node --check content.js` (OK).
83. Следующий шаг для финальной live-валидации: перезагрузить unpacked extension в браузере и повторить цикл перехода (ожидаемо без warning 60с и без повторных автофильтров).
84. После перехода на one-shot логику удалены неиспользуемые helper-функции пост-проверки (`isStageJumpDebtIdAppliedAfterAttempt` и связанные), чтобы исключить путаницу в диагностике.
85. После сообщения пользователя «Перезагрузил и браузер закрыл» выполнен новый чистый live-прогон в новой сессии браузера.
86. Сценарий: `/ovzid/status/all` -> клик в фильтр `ЛС` -> `Enter` -> строки грида загрузились, кнопки `↔` доступны.
87. Выполнен переход по `DebtID=554117`: `↔` -> `Переход к ИД на стадии`; открылась новая вкладка `/ovzid/status/100091`.
88. На целевой вкладке зафиксированы корректные логи one-shot: `Найден параметр DebtID...` -> `DebtID отправлен в фильтр (однократно)` -> `DebtID применен один раз`.
89. После дополнительного ожидания 15 секунд предупреждений нет: `browser_console_messages(level=warning)` вернул 0 сообщений; warning `Не удалось применить DebtID...` не воспроизводится.
90. Проверка состояния jqGrid на целевой странице: `postData._search=true`, `postData.filters={... op:"eq", data:"554117" }`, адрес очищен до `https://kgn.pyramid.vostok-electra.ru/ovzid/status/100091` без `dup_stage_jump_debtid`.
91. Итог: регрессия с повторным циклом после перезапуска расширения не подтверждается; текущая логика `abort + one-shot apply + immediate finish` отрабатывает штатно.
92. По замечанию пользователя выполнена проверка фактических строк грида на целевой стадии, а не только `postData`.
93. Подтверждена проблема: при `postData.filters` с `DebtID=554117` в видимых 30 строках было 30 разных DebtID (фильтрация фактически не применилась).
94. В сети на этой сессии был только запрос `_search=false`; фильтрующий HTTP-запрос с `_search=true` после автофильтра не уходил.
95. Причина: первый one-shot apply мог выполняться слишком рано (до фактического старта/завершения основной загрузки), после чего цикл завершался по факту отправки, а не по факту результата в гриде.
96. В `content.js` добавлена функция `isStageJumpDebtIdResultConfirmed` (подтверждение по реальному результату: `postData` + строки грида или `reccount=0`).
97. `initStageJumpDebtIdFilterFromHash` изменён: теперь нет немедленного `finishSuccess` после первого apply; выполняется ожидание подтверждения результата и допускается один повторный re-apply через 2.5с, без бесконечных циклов.
98. Добавлен блок раннего apply: до 5с ждём факт старта основной загрузки (`sawGridBusy`), чтобы не отправлять фильтр слишком рано на ещё не инициализированный цикл jqGrid.
99. Синтаксис после правки: `node --check content.js` (OK).
100. Для live-проверки новой логики требуется повторная перезагрузка unpacked extension в браузере (текущая вкладка использует уже загруженный content-script).
101. После сообщения пользователя «расширение я обновил» выполнен новый live-прогон: `/ovzid/status/all` -> фильтр `ЛС` (клик + Enter) -> `↔` на строке `DebtID=554117` -> `Переход к ИД на стадии`.
102. На целевой вкладке `/ovzid/status/100091` фильтр фактически применился корректно: в сети ушел `_search=true` с `filters` (`DebtID`, `op=eq`, `data=554117`) и получен `200`.
103. Подтверждение по данным страницы: `reccount=1`, `records=1`, в видимых строках `uniqueDebt=["554117"]` (только нужный долг).
104. Несмотря на корректный результат фильтрации, в консоли воспроизводился ложный warning `Не удалось применить DebtID в течение 60 секунд`, а `dup_stage_jump_debtid` оставался в URL.
105. Причина ложного `fail`: проверка `isStageJumpDebtIdResultConfirmed` была избыточно завязана на `postData`; при неполной синхронизации состояния это блокировало `finishSuccess` даже при уже отфильтрованном гриде.
106. В `content.js` обновлена `isStageJumpDebtIdResultConfirmed`: сначала подтверждение по фактическим строкам (`DebtID` в видимых рядах), затем fallback по `postData`/input и `0` записей (включая текст `Нет записей` в pager).
107. Добавлен helper `hasStageJumpDebtIdInFilterInput` для устойчивой проверки активного фильтра, когда `postData` недоступен/запаздывает.
108. После правки выполнен `node --check content.js` (OK); следующий шаг — повторный live-прогон на обновленной сборке, чтобы подтвердить отсутствие ложного warning и очистку `dup_stage_jump_debtid` в URL.
109. Попытка перезагрузить расширение через Playwright (`chrome://extensions`) не удалась: протокол `chrome:` заблокирован в текущем окружении MCP. Для live-валидации новой правки требуется ручной reload unpacked extension пользователем.
110. По обратной связи пользователя выявлено ухудшение: ожидание полной загрузки до автофильтра и повторная фильтрация (re-apply), что замедляет переход.
111. Причина в логике `initStageJumpDebtIdFilterFromHash`: блок `earlyApplyBlockMs` (ожидание старта загрузки) + `retryAfterApplyMs`/`fallbackApplyDispatched` (повторный автофильтр).
112. Функция `initStageJumpDebtIdFilterFromHash` переписана: короткое окно `abort` (1.5с), затем один `apply` без повторных re-apply; подтверждение результата ожидается до 12с.
113. Удалены старые элементы цикла повторов (`earlyApplyBlockMs`, `retryAfterApplyMs`, `fallbackApplyDispatched`, ожидание `sawGridBusy`), оставлена логика one-shot отправки фильтра.
114. Проверка синтаксиса после полной замены функции: `node --check content.js` (OK).
115. Добавлен pre-abort для ручной фильтрации грида: при `beforeinput` и `Enter` в `input[role="search"]` сначала прерывается активный jqGrid-запрос (загрузка/фильтрация), затем продолжается штатный ввод/поиск.
116. Расширена кнопка `Прервать загрузку` в StageTimer: теперь прерывает любой активный grid-request (включая фильтрацию) и использует `window.stop()` как fallback без привязки к состоянию таймера.
117. Добавлена подмена jqGrid-ошибки после ручного abort: вместо `error` выставляется текст `Загрузка/Фильтрация была прервана пользователем вручную. Повторите действие заново.` (в `content.js` и `StageTimer/timer_content.js`).
118. Проверка синтаксиса после правок: `node --check content.js` и `node --check StageTimer/timer_content.js` — OK.
119. Live-проверка на `/ovzid/PrepareToReturn` (департамент `Курган`): при ручном `ЛС=123` во время `Загрузка` pre-abort в старой логике не срабатывал стабильно (в отдельном прогоне оставался только `_search=false` без фильтра).
120. Доработан `abortStageJumpBusyGridRequest` в `content.js`: добавлены fallback-пути `abort` по всем кандидатам и принудительный `window.stop()` при явном UI-состоянии `Загрузка/Фильтрация`.
121. Повторный live-прогон после правки: сеть зафиксировала `net::ERR_ABORTED` для базового `_search=false`, затем запрос фильтра `_search=true ... data=123` и ответ `200`.
122. Дополнительно из ручной логики убран pre-abort на `beforeinput`; оставлен pre-abort на `Enter`, чтобы исключить лавину повторных прерываний при наборе текста.
123. Проверка синтаксиса после правок: `node --check content.js` — OK.
124. Исправлен сценарий «нужно нажать Enter второй раз»: при pre-abort на `Enter` событие теперь перехватывается (`preventDefault/stopPropagation`) и автоматически переотправляется один раз после короткой задержки (`90мс` для `jqxhr`, `220мс` для `window.stop`), чтобы фильтрация стартовала с первого нажатия.
125. Добавлен аналогичный pre-abort для ручной сортировки по клику на `.ui-jqgrid-sortable`: если грид занят, клик перехватывается, текущий запрос прерывается и этот же клик автоматически переотправляется один раз (задержка `90мс`/`220мс`), чтобы сортировка запускалась с первого клика.
126. Перед доработкой фонового сценария сверился с последним шагом журнала (125).
127. В `content.js` исправлен синтаксис `startStageJumpExecutionAnalysisInBackground` и восстановлено применение визуального состояния кнопки для каждой строки (`loading/success`).
128. В `background.js` добавлены обработчики `STAGEJUMP_OPEN_BACKGROUND_TAB` и `STAGEJUMP_EXECUTION_ANALYSIS_FINISH`, хранение фоновых job и очистка при закрытии source/target вкладок.
129. В `content.js` завершён цикл автодействий для режима `execution_analysis`: выбор строки по `EDocID`, клик `Анализ исполнения`, заполнение и сохранение в модалке.
130. Найдена и исправлена причина таймаута: `isStageJumpElementVisible` не работал с элементами из iframe из-за `instanceof Element`; проверка заменена на `nodeType === 1`.
131. Убраны шумные ошибки канала сообщений при завершении: в `STAGEJUMP_EXECUTION_ANALYSIS_FINISH` статус отправляется сразу, закрытие вкладки выполняется отдельно.
132. Проверка после правок: `node --check content.js` и `node --check background.js` — OK; live-сценарий на `DebtID=4791249`, `EDocID=12051344` выполняется в фоне (вкладка не активируется и закрывается), пользователь подтвердил работоспособность.
133. Перед задачей по мультивыбору сверился с последним шагом журнала (132).
134. Выявлен блокер для пакетного запуска: `execution_analysis` использовал одиночный `localStorage`-payload (`dup_stage_jump_pending`), что приводило бы к гонке при одновременном открытии нескольких фоновых вкладок.
135. В `content.js` для `/ovzid/status/all` возвращены чекбоксы строк (перестали удаляться в `updateStageJumpButtons`), добавлен сбор выбранных строк по `input.cbox` и формирование списка целей `debtId/edocId/targetUrl` для пакетного запуска.
136. В dropdown-пункте `Анализ исполнения` добавлен учёт мультивыбора: динамический счётчик (`N` или `ready/selected`), единый ввод текста один раз и запуск `startStageJumpExecutionAnalysisInBackground` для каждой выбранной строки.
137. Передача payload переведена на background-job: в `STAGEJUMP_OPEN_BACKGROUND_TAB` передаётся `data.payload`, в `background.js` payload валидируется и сохраняется в job; добавлен message `STAGEJUMP_GET_EXECUTION_ANALYSIS_PAYLOAD` для выдачи payload целевой вкладке.
138. В `content.js` `initStageJumpExecutionAnalysisFromStorage` доработан: сначала запрашивает payload у background, затем fallback в storage; сам цикл автозаполнения вынесен в `runStageJumpExecutionAnalysisFlow` с флагом очистки storage только для storage-сценария.
139. Проверка после правок: `node --check content.js` и `node --check background.js` — OK.
140. Перед правкой по запросу «убрать чекбоксы» сверился с последним шагом журнала (139).
141. В `content.js` убраны стили, добавленные под отображение чекбоксов в колонке `cb` для StageJump-кнопки; возвращено исходное центрирование кнопки `↔`.
142. В `updateStageJumpButtons` возвращено удаление `input.cbox/.cbox/label[for^="jqg_list_"]` и очистка лишних узлов в ячейке, чтобы чекбоксы снова не отображались.
143. Логика пакетного выбора для `Анализ исполнения` переведена с `checkbox.checked` на состояние строки (`aria-selected="true"` или класс `ui-state-highlight`), т.е. выбор строк теперь только кликом по строкам.
144. Проверка после правки: `node --check content.js` — OK.
145. Перед ручной проверкой фильтра по ЛС сверился с последним шагом журнала (144).
146. На странице `/ovzid/status/all` в фильтр колонки `ЛС` введено значение `4501000033756` и выполнен поиск по `Enter`; результат — `Просмотр 1 - 6 из 6`.
147. Сетевой запрос фильтра подтверждён: `GET /ovzid/status/data/all?...&_search=true...AccAddress_AccountNumber...data=4501000033756` вернул `200`.
148. Перед доработкой логики закрытия вкладки по success-alert сверился с последним шагом журнала (147).
149. В `content.js` добавлены helper-функции для `execution_analysis`: сбор документов (включая iframe), чтение success-alert `alert alert-success alert-dismissible`, снимок состояния alert до клика `Сохранить информацию` и детектор нового/обновлённого success-alert.
150. В `runStageJumpExecutionAnalysisFlow` после `saveClicked` добавлен приоритетный early-success: как только появляется новый/ставший видимым alert с текстом `Анализ исполнения успешно занес...`, вызывается `finishSuccess()` без ожидания `STAGE_TIMER_STOP`.
151. Для защиты от ложного мгновенного закрытия из-за старого alert добавлен baseline состояния alert до `saveButton.click()` и сравнение с текущим состоянием после клика.
152. Проверка после правки: `node --check content.js` — OK; проверка UTF-8 чтением (`Get-Content -Encoding utf8`) подтверждает корректный русский текст в новой строке детектора success-alert.
153. Перед задачей по pre-abort для dropdown-фильтров сверился с последним шагом журнала (152).
154. Live-проверка на `https://kgn.pyramid.vostok-electra.ru/ovzid/status/all`: `Дата смерти` реализована как нативный `select[role="search"]#gs_list_DeathDate`, `Взыскатель` — как скрытый `select[multiple][role="search"]#gs_list_Claimant_Name` + кнопка `#gs_list_Claimant_Name_ms` и popup `.ui-multiselect-menu`, `Записей на стр.` — как pager-select `.ui-pg-selbox`.
155. В `content.js` добавлен общий pre-abort/replay для ручного выбора в dropdown: перехват `change` у `select[role="search"]` и `select.ui-pg-selbox`, а также перехват кликов по пунктам `.ui-multiselect-menu` (`label`, checkbox, `Выбрать все`, `Убрать все`).
156. При занятом grid-request выбор из dropdown теперь работает по схеме `abort -> preventDefault/stopPropagation -> автоматический replay` того же действия через `90мс` (`jqxhr`) или `220мс` (`window.stop`), чтобы первое мышиное действие сразу запускало нужную фильтрацию/смену количества строк.
157. Дополнительно выделен общий helper синтетического клика для replay и прогнана проверка `node --check content.js` — OK; UTF-8 spot-check по `content.js` подтверждает отсутствие mojibake в затронутом участке.
158. Перед доработкой скрытия по `F1`/скриншоту при открытой карточке сверился с последним шагом журнала (157).
159. Live-проверка на `https://kgn.pyramid.vostok-electra.ru/ovzid/status/all`: после фильтра `ЛС=4501000033756` и открытия `Карточка ИД #12051344` подтверждён баг только при фокусе внутри `iframe` карточки: `F1` и `F8` не скрывают элементы расширения, тогда как при фокусе в верхнем документе скрытие работает.
160. Для исправления добавлен `StageTimer/screenshot_hotkeys_bridge.js`: лёгкий `all_frames`-bridge без UI, который перехватывает `F1`/скриншотные hotkeys внутри `iframe` и отправляет команду в верхний документ через `postMessage`.
161. В `content.js` и `StageTimer/timer_content.js` добавлен приём bridge-сообщений от дочерних `iframe`; в основном расширении команда обрабатывается верхним screenshot-controller, в standalone StageTimer — локальной логикой таймера, без функционального расхождения между сборками.
162. В `manifest.json` и `deploy_VZID.py` подключён новый bridge-скрипт для `all_frames`; в сборку StageTimer добавлено копирование `StageTimer/screenshot_hotkeys_bridge.js`.
163. Перед задачей по `PrintScreen`/`F2` сверился с последним шагом журнала (162).
164. В `content.js` добавлен общий реестр видимости элементов расширения на `chrome.storage.local` и `F2`-диалог с понятными переключателями для подсветок, StageTimer, StageJump, ФССП-группировки, проверки ИНН/смерти и сезонного оформления; состояние общее для всех страниц `*.pyramid.vostok-electra.ru/*`.
165. В `content.js`, `StageTimer/timer_content.js` и `StageTimer/screenshot_hotkeys_bridge.js` исправлено автоскрытие для `PrintScreen`: hotkey теперь передаёт `triggerKey`, а для `PrintScreen` используется отдельное увеличенное окно скрытия (`5с` + продление на blur), чтобы скриншот успевал создаться с выключенными элементами.
166. В `StageTimer/timer_content.js` видимость таймера, кнопки скрытия и кнопки прерывания переведена с `localStorage` на общий `chrome.storage.local`; добавлена синхронизация через `chrome.storage.onChanged` и локальный `F2`-диалог для standalone-сборки без функционального расхождения с основным расширением.
167. В `StageTimer/timer_styles.css` добавлены стили и screenshot-hide для нового окна настроек `F2`, чтобы новые элементы расширения тоже гарантированно скрывались при создании скриншота.
168. Проверка после правок: `node --check content.js`, `node --check StageTimer/timer_content.js`, `node --check StageTimer/screenshot_hotkeys_bridge.js` — OK; проектный `rg -n "Ð|Ñ|Â|�" .` не выявил mojibake в коде, кроме буквальных примеров в `PyramidNewYear/THEME_INTEGRATION_GUIDE.md`.
169. После пользовательской проверки обнаружен пропуск `F2` внутри карточек/вложенных `iframe`: bridge `StageTimer/screenshot_hotkeys_bridge.js` пробрасывал только `F1`/скриншотные hotkeys, поэтому верхний документ не получал команду открытия настроек.
170. Для исправления bridge расширен поддержкой `F2 -> toggle-ui-settings`, а в `content.js` и `StageTimer/timer_content.js` добавлен приём этой команды через `postMessage`; дополнительно убрана зависимость `F2`/`Esc` от `event.defaultPrevented`, чтобы hotkey не терялся из-за page-level обработчиков.
171. Повторная проверка синтаксиса после фикса `F2`: `node --check content.js`, `node --check StageTimer/timer_content.js`, `node --check StageTimer/screenshot_hotkeys_bridge.js` — OK.
172. По уточнению пользователя в `F2`-окне расширен набор переключателей: добавлены `Настройки сезонного оформления` (launcher + overlay/panel) и `Настройка столбцов` (`#jqgrid-manager-btn` + `.jq-ext-modal-overlay`).
173. Новогоднее и весеннее оформление объединены в один общий переключатель `Сезонное оформление`; для мягкой миграции новый ключ `dup_ui_show_seasonal_theme` при первом чтении наследует состояние старых `dup_ui_show_new_year_theme` / `dup_ui_show_spring_theme`, если они были сохранены.
174. Проверка после обновления состава `F2`-настроек: `node --check content.js` — OK.
