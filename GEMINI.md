Всегда пиши на русском языке, комментарий в коде оставяй на русском языке а английские комментарии переводи на русский

## Правила синхронизации кода
- При внесении изменений в логику модуля **StageTimer** (файлы `StageTimer/timer_background.js`, `StageTimer/timer_content.js` или `deploy_VZID.py`), **ОБЯЗАТЕЛЬНО** переноси аналогичные изменения в основные файлы расширения: `background.js` (секция `STAGE TIMER LOGIC`). Логика перехвата URL и обработки событий должна оставаться идентичной в обеих версиях.